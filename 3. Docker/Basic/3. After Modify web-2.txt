তোর ৪টা container আছে → web-1, web-2, payment, db
এখন developer শুধু web-2 container-এ code change করছে।

👉 তখন তোকে পুরো system নামিয়ে আবার সব container চালাতে হবে না।

Step-by-step করবি:

1. ওই container টা rebuild করো
# docker compose build web-2

2. পুরা compose up করার দরকার নাই
শুধু ওইটার জন্য চালাও:
# docker compose up -d web-2

3. Check করো container চলতেছে কিনা
docker ps

✅ এভাবে করলে শুধু web-2 container আপডেট হবে, অন্যগুলো (web-1, payment, db) disturb হবে না।