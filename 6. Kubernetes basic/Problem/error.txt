----- (kubeadm init) not created second time, so it will be remove ----- 

sudo kubeadm reset -f

sudo rm -rf /etc/kubernetes/manifests
sudo rm -rf /var/lib/etcd
sudo rm -rf $HOME/.kube

sudo swapoff -a
sudo sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab

sudo kubeadm init --pod-network-cidr=10.244.0.0/16


if problem, kubeadm init --pod-network-cidr=10.244.0.0/16:
sudo swapoff -a
sudo sed -i '/ swap / s/^/#/' /etc/fstab

sudo systemctl restart kubelet
sudo kubeadm config images pull
kubeadm init --pod-network-cidr=10.244.0.0/16




rm -f crictl-$VERSION-linux-amd64.tar.gz



sudo su
apt-get update -y
apt-get install -y apt-transport-https  === apt-transport-https → এই package টা apt কে HTTPS URL থেকে packages আনতে allow করে।

# Docker install:
apt-get install -y docker.io
docker --version
systemctl enable docker
systemctl start docker
systemctl status docker

# Kubernetes repo add and install:
sudo apt-get update
sudo apt-get install -y apt-transport-https ca-certificates curl gnupg

curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.34/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
sudo chmod 644 /etc/apt/keyrings/kubernetes-apt-keyring.gpg 

echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.34/deb/ /' | sudo tee /etc/apt/sources.list.d/kubernetes.list
sudo chmod 644 /etc/apt/sources.list.d/kubernetes.list  

sudo apt-get update
sudo apt-get install -y kubectl kubelet kubeadm kubectl kubernetes-cni

# Cheack:
which kubectl && kubectl version --client 
which kubeadm && kubeadm version
which kubelet && kubelet --version