# ingress.yaml placeholder
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bidaapi
spec:
  rules:
    - host: bidaapi.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bidaapi
                port:
                  number: 80
